// Supabase の設定。
// このファイルをコピーして `config.js` を作成し、自分のプロジェクトの値を入れてください。
// `config.js` は .gitignore で除外されており、リポジトリにはコミットされません。
window.APP_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-KEY",
};
